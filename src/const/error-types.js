const NAME_OR_PASSWORD_IS_REQUIRED = "name or password is required"

const NAME_UNIQUE = "name is unique"

const USER_NAME_IS_EXISTS = "user name is already exists"

const PASSWORD_IS_ERROR = "password is error"

const AUTHORIZATION = "not authorization"

const UN_PERMISSIONS = "not is unPermissions"

module.exports = {
  NAME_OR_PASSWORD_IS_REQUIRED,
  NAME_UNIQUE,
  USER_NAME_IS_EXISTS,
  PASSWORD_IS_ERROR,
  AUTHORIZATION,
  UN_PERMISSIONS,
}
